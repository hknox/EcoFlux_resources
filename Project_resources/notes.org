Various notes and reminders to myself.

* Backups
** grandwazoo program files
The borg backup script is in /usr/local/libexec/backup_ecoflux.sh.
Borg backup passphrase: ecoflux_blake2
Set up backup_ecoflux.service, backup_ecoflux.timer in /etc/systemd/system.
Backs up to /media/BorgBackup/EcoFlux/.
put the backup file into a systemd timer:
[[https://www.thelinuxvault.net/blog/how-to-schedule-tasks-with-systemd-timers-in-linux/][examples here]]
[[https://www.freedesktop.org/software/systemd/man/latest/systemd.timer.html][freedesktop man page for timers]]
[[https://wiki.archlinux.org/title/Systemd/Timers][Arch Linux docs]]
** live data
To get live data from grandwazoo to sunvillage:
- on grandwazoo and ship to joesgarage, do:
mariadb-dump -u ecoflux -p ecoflux > ecoflux_data_[date].sql ([[https://mariadb.com/docs/server/clients-and-utilities/backup-restore-and-import-clients/mariadb-dump][mariadb dump docs]])
use scp to copy to JoesGarage:
scp hank@grandwazoo.ddns.net:/srv/ecoflux/[filename] .
[[https://github.com/francescozanoni/tutorials/blob/master/mysql_to_sqlite.md][Convert Mysql to sqlite]]
Then load into sqlite db:
rm db.sqlite2
sqlite db.sqlite2 < XXXX.sql

Get media/ files:
scp all of /srv/ecoflux/media to local /media/ dir.
(or use rsync)


* navigataion
Rather than use a strategy where ?next= is used to set the landing
page after an action like add, edit or delete, implement a stack of
recent pages using NavigationHistoryMiddleware in middleware.py.
Every time we arrive on a page, push the link onto the navigation
stack. When it's time to get_success_url, use
navigation.py/get_return_url() to get the previous page to return to.
Very simple and elegant solution.


* Sara's sites
[[https://carbonique.ca/en/data.html][Carbonique]]
[[https://ecoflux-lab.github.io/Data.html][EcoFlux main site]]


* Bootstrap accordion notes
reorganize _detail pages to have ONE master accordion header then
individual accordions for each object underneath so only one is open
at a time.

Accordion class Core Structure

An accordion in Bootstrap 5 uses a specific HTML structure with these key elements:
Element	        Class	                Purpose
Container	.accordion	        Wraps all accordion items
Item	        .accordion-item	        Container for a single panel
Header	        .accordion-header	Contains the clickable button
Button	        .accordion-button	The clickable trigger (can be inside or be the header)
Body	        .accordion-body	        Contains the collapsible content


* [[https://chatgpt.com/c/687295f7-4304-8001-b080-4f2e315195eb][ChatGPT discussions]]


* Favicon editing
rebuild it using the present logo without the white border:
https://gist.github.com/jfrobbins/7a579fe888b08612f892db759db2ed13


* .gitignore/git includes
- **migrations should be in Git** ✅

1. **Track changes to the database schema over time**
   Just like code, migrations document what changed in your models and when.

2. **Ensure all team members (or production servers) run the same schema**
   If migrations aren’t in version control, others can’t apply the same schema changes without guessing.

3. **Enable consistent deployment**
   When deploying (e.g., to production), the migration files ensure Django knows exactly what schema to apply.

---

### 🧹 Optional Exception (Don’t Commit These)

- You *can* ignore auto-generated migration files **during heavy model prototyping**, especially if you're throwing them away frequently — but only in isolated branches.
- Once the schema stabilizes, they must be committed.


### summary

| File                                  | Commit to Git?                       |
| ------------------------------------- | ------------------------------------ |
| `Pipfile` / `requirements.txt`        | ✅ Yes                                |
| `migrations/*.py` (not `__pycache__`) | ✅ Yes                                |

Let me know if you’d like a `.gitignore` template or a typical migration folder structure explanation.


* Rich text editor
For adding formatting attributes to text fields.
*** Primary options seem to be TinyMCE and CKEditor

Duck.ai: Bottom Line,
For internal, trusted users, just use TinyMCE without additional
sanitization. It's designed to prevent accidental HTML mistakes, and
that's all you need. You're overthinking it for your use case!
Focus your energy on the image upload validation (file type, size
limits) instead — that's where real issues can occur.

GitHub CoPilot: If you're dealing with ecological/scientific content
with potential tables, images, and complex formatting, CKEditor would
be the better choice. If it's simpler content (blog posts,
descriptions), TinyMCE would be more efficient. Both have solid Django
integration, built-in sanitization, and active maintenance—so you
won't have the deprecation issues that Summernote has with bleach.
Given your simple requirements, TinyMCE is the clear winner for your
use case.
*** TinyMCE
The PyPi package seems outdated. I ended up doing a self-hosted
install of the most recent version as of [2026-03-15 Sun].

[[https://github.com/jazzband/django-tinymce][PyPi package django-tinymce]]

For Tiny Cloud installation:
<!-- Place the first <script> tag in your HTML's <head> -->
<script src="https://cdn.tiny.cloud/1/9c0gbyf3bxmxaijs000y7riwfaeh6bm3ezxvfrs6yd84j0hn/tinymce/8/tinymce.min.js" referrerpolicy="origin" crossorigin="anonymous"></script>

API Key (for Tiny Cloud install)
9c0gbyf3bxmxaijs000y7riwfaeh6bm3ezxvfrs6yd84j0hn



[[https://www.tiny.cloud/docs/tinymce/latest/][TinyMCE official documentation]]
[[https://django-tinymce.readthedocs.io/en/latest/][django-tinymce documentation]]

[[https://unwiredlearning.com/blog/django-rich-text-editor][Overview 1 of integration]]
[[https://www.iditect.com/programming/python-example/how-to-integrate-custom-rich-text-editor-in-your-django-website.html][Overview 2 of integration]]

*** CKEditor
[[https://ckeditor.com/][Home]]
[[https://django-ckeditor.readthedocs.io/en/latest][CKEDitor online docs]]

*** Other options:
[[https://github.com/Pascal-ai-dev/django-textedit][Django Textedit]]
[[https://github.com/agusmakmun/django-markdown-editor?tab=readme-ov-file][Martor]]
[[https://github.com/neutronX/django-markdownx][MarkdownX]]
[[https://github.com/timmyomahony/django-pagedown][Django Pagedown]] (Stack Overflow's PageDown editor)
[[https://github.com/lqez/django-summernote?tab=readme-ov-file][Summernote]] (older, needs sanitization)


* GenericForeignKeys
Used to associate Documents with Equipment, Fieldnote or Site. From
Duck.AI, the fields of a model which implement Generic Foreign Keys
are:
ContentType	        Stores which model type (Equipment, Site, or Fieldnote) the document relates to
object_id	        Stores the primary key of that specific object
GenericForeignKey	Not a real database field—it's a convenience accessor that combines the above two to let you access the actual related object

-- Important: GenericForeignKey is not a database field! Only
content_type and object_id are actual database columns. --


* [[https://www.freecodecamp.org/news/how-to-change-your-django-secret-key-without-breaking-your-app][﻿How to replace SECRET_KEY, development and deployment]]
```
from django.core.management.utils import get_random_secret_key
print(get_random_secret_key())
```
Goes into .env


* How to manage settings.py for development and deployment

[[https://pypi.org/project/python-decouple/#python-decouple-strict-separation-of-settings-from-code][Use decouple]]

From OpenAI's GPT-4o mini:

See also [[Deployment][deployment notes]].

To manage files that differ between development and deployment in a
Django project, such as `settings.py`, while using Git, you can follow
these best practices:

** DONE Use Environment Variables
Instead of hardcoding sensitive information or environment-specific
settings in `settings.py`, use environment variables. This allows you
to keep your settings flexible and secure.
*** DONE Install `python-decouple`
*** DONE Create a `.env` file
In your project root, create a `.env` file to store your environment
variables.
*** DONE Modify `settings.py`
   Use `decouple` to read from the `.env` file.
   ```python
   from decouple import config

   DEBUG = config('DEBUG', default=False, cast=bool)
   SECRET_KEY = config('SECRET_KEY')
   DATABASE_URL = config('DATABASE_URL')
   ```
*** DONE Add `.env` to `.gitignore`

** DONE Environment variable matrix
All of these should be set in .env and picked up in settings.py:

|------------------------------------+--------------------+----------------+--------------------------|
| setting                            | [[https://github.com/django/django/blob/main/django/conf/global_settings.py][defaults]]           | devel          | prod                     |
|------------------------------------+--------------------+----------------+--------------------------|
| DJANGO_SETTINGS_MODULE/not setting | must set!          | TBA            | same as devel?           |
|------------------------------------+--------------------+----------------+--------------------------|
| SECRET_KEY                         | ""                 | must set       | must set                 |
| DEBUG                              | False              | True           | False                    |
|------------------------------------+--------------------+----------------+--------------------------|
| EMAIL_HOST                         | "localhost"        | specific       | videotron                |
| EMAIL_PORT                         | 25                 | specific       | videotron                |
| EMAIL_HOST_USER                    | ""                 | specific       | videotron                |
| EMAIL_HOST_PASSWORD                | ""                 | specific       | videotron                |
| EMAIL_USE_SSL                      | False              | specific       | videotron                |
|------------------------------------+--------------------+----------------+--------------------------|
| MAINTENANCE_MODE                   | None               | settable       | settable                 |
| MAINTENANCE_LOGOUT                 | False              | settable       | settable                 |
| MAINTENANCE_MODE_IGNORE_SUPERUSER  | False              | settable       | settable                 |
|------------------------------------+--------------------+----------------+--------------------------|
| MEDIA_URL                          | ""                 | /media/        | specific                 |
| LOGIN_URL                          | "/accounts/login/" | default        | specific                 |
| STATIC_URL                         | None               | static/        | specific                 |
|------------------------------------+--------------------+----------------+--------------------------|
| ADMINS                             | []                 | []             | specifc                  |
| ALLOWED_HOSTS                      | []                 | ["localhost",] | specific                 |
|------------------------------------+--------------------+----------------+--------------------------|
| DATABASES                          | {} (dummy backend) |                |                          |
| ENGINE                             |                    | (sqlite)       | (mariadb)                |
|------------------------------------+--------------------+----------------+--------------------------|
| DB_ENGINE                          | not Django         | "...sqlite3"   | "..mysql"                |
| DB_NAME                            | not Django         | "db.sqlite3"   | specific                 |
| DB_USER                            | not Django         | ""             | specific                 |
| DB_PASSWORD                        | not Django         | ""             | specific                 |
| DB_HOST                            | not Django         | ""             | specific                 |
| DB_PORT                            | not Django         | ""             | specific                 |
|------------------------------------+--------------------+----------------+--------------------------|
| FORCE_SCRIPT_NAME                  | None               | None           | /ecoflux                 |
| WSGI_APPLICATION                   | None               | None           | EcoFlux.wsgi.application |
|------------------------------------+--------------------+----------------+--------------------------|

** DEFERRED Use Git Branches

If your development and production environments are significantly
different, consider using separate branches for each environment. This
allows you to manage changes independently.

*** Create a branch for development
   ```bash
   git checkout -b development
   ```

*** Create a branch for production
   ```bash
   git checkout -b production
   ```

*** Merge changes as needed
When you have stable changes in development, merge them into production.


* Testing deployment
** Get current data to a test db:
[create testing_ecoflux, grant privileges to user ecoflux]
mysqldump -u username -p --add-drop-database source_db | mariadb -u username -p testing_ecoflux

Maybe better to dump to a file then load from the file:
mysqldump -u username -p source_db --add-drop-database > datadump.sql
mariadb -u username -p testing_ecoflux < datadump.sql
** uninstall django-tinymce
** get current git branch
** collectstatic

* Deployment
[[https://absolutecodeworks.com/django-web-application-crud-sample-using-mariadb][Example setup for deployment]]
** Managing credentials, devel vs prod settings
Excellent questions — these are core concerns when building a Django
app that’s cleanly portable across **development**, **staging**, and
**production** environments.

Let’s tackle your two questions:


*** ✅ Q1: How to securely handle secrets like MariaDB credentials in `settings.py`?

- Best practice: **Don't hardcode secrets in `settings.py`**. Use
  environment variables instead and read them in `settings.py`:

```python
import os

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': os.environ.get('DB_NAME', 'default_name'),
        'USER': os.environ.get('DB_USER', 'default_user'),
        'PASSWORD': os.environ.get('DB_PASSWORD', 'default_pass'),
        'HOST': os.environ.get('DB_HOST', 'localhost'),
        'PORT': os.environ.get('DB_PORT', '3306'),
    }
}
```

- In development: set them via `.envrc` or `.env` with `direnv`.
- In production: set them in your Apache virtual host config, or a WSGI wrapper script, or a systemd unit if you're using Gunicorn later.

*** ✅ Q2: How to handle environment-specific settings (dev vs prod) — and what to put in Git?

### 🔥 Best practice: split settings into **base + environment-specific** files.

```
config/
├── __init__.py
├── settings/
│   ├── __init__.py         # usually empty
│   ├── base.py             # shared by all environments
│   ├── dev.py              # development overrides
│   └── prod.py             # production overrides
```

**Example:**

**base.py:**

```python
# settings/base.py

DEBUG = False
ALLOWED_HOSTS = []
INSTALLED_APPS = [... base apps ...]

# Load from environment variables:
SECRET_KEY = os.environ["DJANGO_SECRET_KEY"]
```

**dev.py:**

```python
from .base import *

DEBUG = True
ALLOWED_HOSTS = ["localhost", "127.0.0.1"]
SECRET_KEY = "insecure-dev-key"

EMAIL_BACKEND = "django.core.mail.backends.console.EmailBackend"
```

**prod.py:**

```python
from .base import *

DEBUG = False
ALLOWED_HOSTS = ["example.com"]

# Secret key comes from environment
SECRET_KEY = os.environ["DJANGO_SECRET_KEY"]
```

**In `.envrc` or Apache env:**

```bash
export DJANGO_SETTINGS_MODULE=config.settings.dev  # or config.settings.prod
```

---

### ✅ What to commit to Git?

**Commit:**

- `config/settings/base.py`, `dev.py`, `prod.py`
- A sample `.envrc` or `.env.example` for local development (no real secrets)

**Don't commit:**

- `.envrc` or `.env` with real secrets
- `.pyc` files, obviously

---

### 🔐 Optional: use `python-decouple` or `django-environ` to streamline

For more structure and type conversion, you can use packages like:

- [`python-decouple`](https://pypi.org/project/python-decouple/)
- [`django-environ`](https://pypi.org/project/django-environ/)

But raw `os.environ.get()` is totally fine and understandable.

---

## 🧩 Summary

| Concern                   | Best Practice                                      |
| ------------------------- | -------------------------------------------------- |
| **Secrets**               | Use environment variables (set via `.envrc`, etc.) |
| **Multiple environments** | Split settings into `base.py`, `dev.py`, `prod.py` |
| **Version control**       | Commit example env files; ignore actual secrets    |
| **Ease of switching**     | Use `DJANGO_SETTINGS_MODULE` in `.envrc`           |

---

Let me know if you want a starter `settings` layout scaffold or a `.envrc` template — I can build one around your current structure.

** Database
*** Using MariaD on grandwazoo as a test case.
[[https://www.digitalocean.com/community/tutorials/how-to-use-mysql-or-mariadb-with-your-django-application-on-ubuntu-14-04][
Using MariaDB with Django app]]

[[https://mariadb.com/docs/server/mariadb-quickstart-guides/mariadb-remote-connection-guide][MariaDB doc for remote access]]

see /etc/mysql/mariadb.conf.d/50-server.cnf:

From DuckAI:

"""Using a hostname can be beneficial in environments with dynamic IPs,
but it may introduce a slight delay during the resolution process...
...you do not need to restart the MariaDB server if your dynamic
hostname's IP address changes. MariaDB will automatically resolve the
hostname to the new IP address when connections are made...
...if you want MariaDB to listen on both the specific hostname and
localhost, you would typically use:
  bind-address = 0.0.0.0"""



Use MariaDB only from localhost - install EcoFlux app on sunvillage.

No need to modify bind-address.
Create EcoFlux db (ecoflux)
  CREATE Database ECOFLUX;
Create EcoFlux user and grant privileges
  CREATE USER 'ecoflux'@localhost'' IDENTIFIED BY [password];
  GRANT ALL PRIVILEGES ON ecoflux.* to 'ecoflux'%'localhost';
  FLUSH PRIVILEGES;
  Test by logging on as ecoflux: OK

To use ed25519 authentication, you need to do:
INSTALL SONAME 'auth_ed25519';
every time you reinstall MariaDB or restore from a backup prior to
when I ran it today ([2026-07-18 Sat])
*** Root access to MariaDB
Simplest: sudo mariadb
will log straight into mariadb as root.

[[https://www.simplified.guide/mysql-mariadb/root-reset-password][Reset MariaDB root password]]

** Webserver
*** Apache
**** Installation
SUMMARY from Chat:
You don’t need to install your Django code inside /var/www/html/. Use
a dedicated directory like /srv/ecoflux/, keep ownership as yourself,
and let Apache (via mod_wsgi) serve it securely. pipenv and the
www-data user don’t need to mix — mod_wsgi will handle activating your
virtualenv when configured correctly.

chown -R hank:www-data /srv/ecoflux/
**** Permissions
Best Practices:
Keep ownership as your user (not www-data or root)

Ensure the Apache www-data user can read the Django code and activate
the virtualenv (you’ll do this via mod_wsgi settings)
**** Checklist
***** PREPARE WORKING ENVIRONMENT
x install envrc
x install pipenv
x sudo mkdir /srv/ecoflux/
x cd /srv/ecoflux/
x clone git repo
x chown -R hank:www-data .
x pipenv install
***** CONFIGURE MYSQL ACCESS
x pipenv install mysqlclient (might need to "sudo apt install
  default-libmysqlclient-dev")

x change settings.py and see Chat re/settings.py ideas

Let me know if you want help with:
- splitting settings.py for dev vs prod
- using .env or .envrc for secrets like DB credentials

TEST: python manage.py migrate: OK
Bonus: I loaded fixtures from JoesGarage.

***** CONFIGURE APACHE:
X aptitude install libapache2-mod-wsgi-py3
x a2enmod wsgi
x systemctl restart apache2

create /etc/apache2/sites-available/ecoflux.conf
a2ensite ecoflux.conf
restart apache
python manage.py collectstatic
    setting up mod_wsgi to use the virtualenv properly

**** DONE Changes to settings.py
DATABASES = {
   'default': {
       'ENGINE': 'django.db.backends.mysql',
       'NAME': '<your_database_name>',
       'USER': '<your_database_user>',
       'PASSWORD': '<your_database_password>',
       'HOST': 'localhost',
       'PORT': '',
   }
}
FORCE_SCRIPT_NAME = '/myapp'

STATIC_URL = '/myapp/static/'

**** VirtualHost conf


**** Gunicorn -for later
reverse proxy to Apache or Nginx
** Proxy setup
I have set up SunVillage as the front end webserver for my network. It
will handle the NextCloud site directly and pass ecoflux/ and
testing_ecoflux/ to Phydeaux3. This backend server listens on port
8080 and doesn't need SSL certificates because all the traffic is
local.

For the proxied sites, you need to set up CORS to allow
cross-scripting to the proxied site. [[https://github.com/adamchainz/django-cors-headers][Docs are here.]]

From Claude:

"When you served /testing_ecoflux/ directly from the front-end server
(without a proxy), the browser made requests to
https://grandwazoo.ddns.net/testing_ecoflux/. The origin was
https://grandwazoo.ddns.net, and the Django app was also running on
https://grandwazoo.ddns.net—same origin. No CORS check happens for
same-origin requests.

Now that you're proxying through a reverse proxy to the backend, the
browser still sees requests coming from https://grandwazoo.ddns.net
(thanks to ProxyPreserveHost On), but the backend Django app is
actually running on http://192.168.1.120:8080. When the browser makes
a POST request, Django sees the Origin header as
https://grandwazoo.ddns.net while the app is "listening" on a
different host/port, so it triggers the CORS check.

The proxy doesn't automatically pass through CORS headers—it passes
through the request and response, but the CORS validation happens
inside Django itself before any response is sent.

So installing and configuring django-cors-headers is the right move.
Once you add "https://grandwazoo.ddns.net" to CORS_ALLOWED_ORIGINS,
the POST requests should work."


* Forms
** Goals
control over layout
control over widgets and behaviour
catch cancel/navigation when page is dirty
add DOI records
add field note records
** [[https://docs.djangoproject.com/en/5.2/topics/][Main Documentation page]]
** Titles for an input yield help text for that input
** Adding attributes to input widgets using crispy Layout
Field: can add arbitrary attributes to input widgets using keyword
assignments, eg 'rows=4', 'cols=25', etc.
** multipart/form-data
This is automatically set when the form needs this enctype, so there
is no official crispy-layout helper attribute for it.

** [[https://www.youtube.com/watch?v=X8ECf3Ow3ww][Intro to crispy-forms (YouTube)]]
** FormHelper attributes
From ChatGPT:
🔍 What do all these helper attributes do?
Attribute	Purpose
form_class	Applied to the <form> element. Common value: "row g-3" for Bootstrap 5 horizontal layout.
label_class	Applied to all <label> elements. Controls label width and alignment.
field_class	Applied to the container <div> around each field (not the input itself). Controls how wide the field area is.
wrapper_class	Applied to the outer wrapper div of each field-label pair (the whole row). Optional but useful for spacing (mb-3, etc.).

(Summary 2:)
form_class: "row g-3" sets Bootstrap horizontal layout with spacing
label_class: aligns and sizes labels (left column)
field_class: sizes input area (right column)

wrapper_class="mb-3": adds vertical spacing between rows (optional but nice)
🔄 Summary of customization points
    form_class: "row g-3" sets Bootstrap horizontal layout with spacing
    label_class: aligns and sizes labels (left column)
    field_class: sizes input area (right column)
    wrapper_class="mb-3": adds vertical spacing between rows (optional but nice)

✅ Summary: What can be overridden per field?
Attribute	Set Globally (on FormHelper)	Override Per Field (Field(...))
form_class	✅ Yes	🚫 No (applies to <form>)
label_class	✅ Yes	✅ Yes (via Field(..., wrapper_class=...) or css_class tricks)
field_class	✅ Yes	✅ Yes (Field(..., wrapper_class=...))
wrapper_class	✅ Yes	✅ Yes (Field(..., wrapper_class=...))

Note: form_class applies to the <form> element, so it's always global.

🧠 Bonus: You can also use Div(...) for more control
This is handy for grouping fields or overriding column layout per row.

Adding form-control to all fields:
Method	Best Use Case
__init__() loop	                        Medium/large forms with consistency
Meta.widgets	                        Few fields, specific overrides
crispy-forms + {% crispy form %}	Automatic Bootstrap styling
** ChatGPT overview of dirty-form protection  options
...what you’re describing is a pretty common UX pain point with Django
“detail + related records” pages, and you’re right that just making
Equipment or FieldNotes part of the main Site formset is the wrong fit
if you need their full create forms.

There are a few patterns that work well here, and the “dirty form”
warning is the main challenge to solve.

*** 1. Open the Create form in a modal (Bootstrap modal or similar)**

- You can keep the Site form open and “dirty” in the background.
- Clicking “Add Equipment” triggers JavaScript to load the `CreateView` form into a Bootstrap modal via AJAX.
- When the modal form is submitted (via AJAX), the server:

  * Creates the Equipment or FieldNote instance.
  * Returns a JSON response with the rendered row for the updated list (or the updated list HTML).
  * JavaScript then injects the new row into the Equipment or FieldNote table without a full page reload.
 **Benefits**: No dirty form warning, user stays on page.
 **Downsides**: You have to AJAXify your CreateView (but you can keep the regular non-AJAX view for fallback).

*** 2. Open Create form in a new browser tab**

- “Add Equipment” link uses `target="_blank"`.
- User fills out the new item in a separate tab.
- After saving, they close the tab and return to the original Site page — which still has their unsaved changes.
 **Benefits**: No dirty form prompt.
 **Downsides**: Slightly clunky UX; users must be comfortable with multiple tabs.

*** 3. Save the Site form automatically before opening Create**

- When “Add Equipment” is clicked:

  * JavaScript first sends an AJAX POST to save the Site form.
  * If successful, it redirects to the CreateView for Equipment/FieldNote, already knowing the saved Site PK.
 **Benefits**: Keeps the workflow single-page without modal complexity.
 **Downsides**: Might commit partially-complete Site data before the user is ready.

*** 4. Use an inline modal form but *not* a formset**

- Instead of a separate CreateView, render a full form for Equipment/FieldNote *inside* a modal on the Site page.
- Submit it via AJAX to the standard CreateView endpoint.
- Refresh the relevant list with the new item after save.
 **Benefits**: Keeps full control of fields; no partial formset.
 **Downsides**: You need to load the form via AJAX and re-render errors if validation fails.

*** Recommendation for your case**

Given:

- You want the full CreateView fields for Equipment/FieldNote.
- You don’t want to lose Site form changes.
- You already have a dirty-form warning system.

I’d go with option 1** — Bootstrap modal + AJAX form submission.
It avoids the dirty form prompt, keeps UX smooth, and works with your existing CreateView with only a little refactoring to handle AJAX.

But in the end I went with opton 3, save dirty site

** Container margins, gaps, padding
default: containers have left and right padding
** [[https://docs.djangoproject.com/en/5.2/topics/forms/formsets/][Formsets]]
** [[https://docs.djangoproject.com/en/5.2/topics/forms/modelforms/#model-formsets][Modelformsets]]
** [[https://docs.djangoproject.com/en/5.2/ref/forms/widgets/][Django widget docs]]
How to add CSS classes to widgets in Python
** [[https://docs.djangoproject.com/en/5.2/ref/forms/api/#binding-uploaded-files-to-a-form][Uploading files to a form]]




* Bootstrap notes


* MDN exploration
** To pick up later
- Forms and buttons
- Table challenge: Planets Data





* novalidate
Re: 'novalidate' attribute: See https://docs.djangoproject.com/en/5.2/topics/forms/:

"If your form includes a URLField, an EmailField or any integer field
type, Django will use the url, email and number HTML5 input types. By
default, browsers may apply their own validation on these fields,
which may be stricter than Django’s validation. If you would like to
disable this behavior, set the novalidate attribute on the form tag,
or specify a different widget on the field, like TextInput."

Without it the saved-data check fails to run.



* HTML notes
Using an href="?site=4.." reloads the current site using this as a
query string.


* [[https://learndjango.com/tutorials/django-favicon-tutorial][Adding favicons in Django]]


* template hierarchies
common template inheritance hierarchy
- Create a base.html template that holds the main look-and-feel of your site.
- Create a base_SECTIONNAME.html template for each “section” of your
  site. For example, base_news.html, base_sports.html. These templates
  all extend base.html and include section-specific styles/design.
- Create individual templates for each type of page, such as a news
  article or blog entry. These templates extend the appropriate
  section template.
** bootstrap5 hierarchies
   [[https://docs.djangoproject.com/en/5.1/ref/templates/language/][Documentation: template language]]
- django_bootstrap5/bootstrap5.html: basic bootstrap configuration
  offers several blocks to override:
  - bootstrap5_title
  - bootstrap5_before_content
  - bootstrap5_content
  - bootstrap5_after_content
  - bootstrap5_extra_script
  templates further down set the above variables
** django_bootstrap-example hierarchies
- bootstrap.html extends django_boostrap5/bootstrap5.html
  offers title block and sets bootstrap5_title to title
- base.html extends bootstrap.html
  offers blocks to override:
  - title
  - content
  Sets up a navigation bar with links, NOT fixed position (floats with
  content), shows any bootstrap_messages (set in view)
 - home.html extends base.html
   sets title, content blocks




* Configuration
#+TODO: TODO(t) NEXTACTION(n) STARTED(s) WAITING(w) | DONE(d) CANCELED(c) DEFERRED(f)
